#include<stdio.h>
#include<stdlib.h>

#define WIDTH 20
#define HEIGHT 20
#define MAX_SHAPES 50

typedef enum{
    RECTANGLE,
    LINE,
    TRIANGLE,
    CIRCLE
}ShapeType;

typedef struct{
    int id;
    ShapeType type;
    int x1,y1,x2,y2,x3,y3;
    int is_active;
}Shape;

char canvas[HEIGHT][WIDTH];
Shape shape_list[MAX_SHAPES];
int shape_count=0;

